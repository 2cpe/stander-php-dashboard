# stander-php-dashboard

A small PHP admin dashboard I wrote while learning backend
development.

## What it is

A basic admin interface for working with data through a web UI.
Three files: an admin panel, a main dashboard view, and a
database connection layer.

## Status

Rough in places — this is a learning project, not a production
tool. Configure `db.php` with your own database credentials
before running.