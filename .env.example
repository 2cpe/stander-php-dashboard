# Copy this file to `.env` and fill in your values.
# `db.php` reads these to connect to MySQL.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=anasx

# Session hardening (optional, recommended for production)
# SESSION_COOKIE_SECURE=1
# SESSION_COOKIE_HTTPONLY=1